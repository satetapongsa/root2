chmod u+rwx [file]
sudo chown alice report.txt
cat /etc/passwd [username : password : UID : GID : GECOS : home_directory : shell] [root:x:0:0:root:/root:/bin/bash]
grep "root" /etc/passwd
sudo useradd [OPTION] USERNAME / sudo useradd -m -s /bin/bash student
sudo passwd USERNAME / sudo passwd student
sudo usermod -s /bin/zsh student
sudo userdel [OPTION] USERNAME
getent passwd student
sudo groupadd developers
sudo groupmod -n newgroup developers
sudo groupdel GROUP
